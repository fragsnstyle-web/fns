/* ═══════════════════════════════════════════════════════
   CONFIG.JS — Configuración pública de la tienda
   
   Este archivo se actualiza AUTOMÁTICAMENTE cuando el
   admin completa la configuración inicial en el panel.
   
   NO edites este archivo manualmente.
═══════════════════════════════════════════════════════ */

window.FYE_CONFIG = {
  // ID del almacenamiento en la nube (JSONBin).
  // Se establece automáticamente al completar la configuración.
  binId: '',

  // Versión — no modificar
  v: '3.1'
};
